export interface SigninTypes {
    email: string;
    password: string;
};
export interface SignupTypes {
    email: string;
    confEmail: string;
    password: string;
    confPassword: string;
    remeberMe: boolean;
};
